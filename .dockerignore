# ============================================================================
# 构建上下文排除清单
#
# 关键作用是排除 node_modules（本机约上百 MB），
# 依赖在镜像内由 npm ci 依据 package-lock.json 重新安装，保证可重现。
# ============================================================================

node_modules/
dist/
dist-ssr/

# 版本控制与 IDE
.git/
.gitignore
.idea/
.vscode/
*.local
.DS_Store

# 日志
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Docker 自身
Dockerfile
.dockerignore
